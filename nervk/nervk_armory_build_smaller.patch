--- /tmp/build_old.sh	2026-06-12 19:27:30.370016506 +0000
+++ /tmp/build_new.sh	2026-06-12 19:27:30.384008536 +0000
@@ -1,6 +1,6 @@
 #!/bin/sh
-# .nervk armory dlopen tiny build: SDL/GL/libm are resolved at runtime.
-# Size-obsessed: only libc remains as DT_NEEDED on glibc >= 2.34.
+# .nervk armory dlopen tiny build: compression-aware goblin variant.
+# SDL/GL/libm are resolved at runtime; only libc remains as DT_NEEDED.
 set -eu
 
 SRC=${1:-nervk_armory_dlopen.c}
@@ -10,8 +10,8 @@
 TIN=${TIN:-$OUT.xzrun}
 HERE=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
 
-CFLAGS="-Os -Wall -fno-asynchronous-unwind-tables -fno-unwind-tables -ffunction-sections -fdata-sections -fno-stack-protector -fomit-frame-pointer -fmerge-all-constants -fno-math-errno -fno-ident"
-LDFLAGS="-Wl,--gc-sections -Wl,--build-id=none -Wl,-z,norelro -Wl,-z,noseparate-code -Wl,--as-needed -Wl,--hash-style=sysv"
+CFLAGS="-Oz -Wall -flto -fwhole-program -fno-plt -no-pie -fno-toplevel-reorder -fno-reorder-functions -fno-schedule-insns -fno-schedule-insns2 -fno-ipa-cp -fno-ipa-sra -fno-tree-sra -fno-expensive-optimizations -fno-asynchronous-unwind-tables -fno-unwind-tables -ffunction-sections -fdata-sections -fno-stack-protector -fomit-frame-pointer -fmerge-all-constants -fno-math-errno -fno-ident"
+LDFLAGS="-flto -no-pie -Wl,--gc-sections -Wl,--build-id=none -Wl,-z,norelro -Wl,-z,noseparate-code -Wl,--as-needed -Wl,--hash-style=sysv"
 
 if command -v pkg-config >/dev/null 2>&1 && pkg-config --exists sdl2 && [ -e /usr/include/GL/gl.h ]; then
   SDL_CFLAGS=$(pkg-config --cflags sdl2)
@@ -34,15 +34,15 @@
 SST_SZ=$(stat -c%s "$SST")
 printf '[build] sstrip:    %s bytes\n' "$SST_SZ"
 
-xz -9e --check=crc32 --lzma2=preset=9e,pb=0,lc=4 -c "$SST" > "$SST.xz"
+xz -9e --check=none --lzma2=preset=9e,pb=0,lc=0 -c "$SST" > "$SST.xz"
 XZ_SZ=$(stat -c%s "$SST.xz")
 printf '[build] xz stream: %s bytes\n' "$XZ_SZ"
 
 cat > "$TIN" <<'STUB'
 #!/bin/sh
-t=${TMPDIR:-/tmp}/n.$$
-tail -n +5 "$0"|xz -dc >"$t"&&chmod +x "$t"&&"$t" "$@"
-r=$?;rm -f "$t";exit $r
+t=/tmp/n.$$
+tail -n+4 $0|xz -dc>$t&&chmod +x $t&&$t "$@"
+r=$?;rm $t;exit $r
 STUB
 cat "$SST.xz" >> "$TIN"
 chmod +x "$TIN"
